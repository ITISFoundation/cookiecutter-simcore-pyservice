language: python
addons:
  apt:
    packages:
      - "python3"
      - "python3-pip"
python:
  - "3.6"
install: 
  - "pip install --upgrade pip wheel setuptools"
  - "pip install -r requirements-dev.txt"
script: pytest
